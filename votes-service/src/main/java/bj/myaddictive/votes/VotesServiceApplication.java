package bj.myaddictive.votes;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/** Rubrique 4, "Competitions et votes" (section 7) : phases, candidats, portefeuille de pieces, votes payants. */
@SpringBootApplication
public class VotesServiceApplication {
    public static void main(String[] args) {
        SpringApplication.run(VotesServiceApplication.class, args);
    }
}
